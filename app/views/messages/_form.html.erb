<%= form_with(model: message, local: true) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :from_uid %>
    <%= form.text_field :from_uid %>
  </div>

  <div class="field">
    <%= form.label :to_uid %>
    <%= form.text_field :to_uid %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :is_read %>
    <%= form.check_box :is_read %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
